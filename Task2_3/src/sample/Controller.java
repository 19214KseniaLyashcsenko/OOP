package sample;

public class Controller {
}
/*
Button btn = new Button("Click");
        btn.setPrefWidth(80);

        Scene a = scene;
        btn.setOnAction(new EventHandler<ActionEvent>() {

            @Override
            public void handle(ActionEvent event) {
                startGame(primaryStage, a);
            }
        });

        BorderPane root = new BorderPane(btn);

        scene = new Scene(root, width * cornersize, height * cornersize);

        primaryStage.setScene(scene);

        primaryStage.setTitle("Hello JavaFX");

        primaryStage.show();

        switch (foodColor.get(i)) {
                case 0:
                    cc = Color.rgb(255, 69, 69);
                    break;
                case 1:
                    cc = Color.rgb(255, 252, 66);
                    break;
                case 2:
                    cc = Color.rgb(142, 255, 66);
                    break;
                case 3:
                    cc = Color.rgb(66, 255, 220);
                    break;
                case 4:
                    cc = Color.rgb(174, 60, 250);
                    break;
            }

            gc.setFill(cc);
            gc.fillOval(foodX.get(i) * cornersize, foodY.get(i) * cornersize, cornersize, cornersize);
 */